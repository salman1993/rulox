// Define a function to calculate factorial
fun factorial(n) {
  var result = 1;
  var i = n;
  
  while (i > 1) {
    result = result * i;
    i = i - 1;
  }
  
  return result;
}

// Main execution
var num = 10; // Change this number as needed
print "Factorial of " + num;
print factorial(num);
